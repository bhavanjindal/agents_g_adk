{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A 2>/dev/null; git diff --cached --quiet || git commit -m \"auto-save: $(date '+%Y-%m-%d %H:%M')\"; git remote get-url origin > /dev/null 2>&1 && git push || true",
            "statusMessage": "Committing and pushing changes to GitHub..."
          }
        ]
      }
    ]
  }
}
